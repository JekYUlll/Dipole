# Dipole Settings v1

设置页只承载已有权威接口支持的个人签名、同步状态、设备安全入口和退出登录。设备会话详情继续由 `Device Security` 页面处理，设置页不披露 IP、节点、连接 ID 或原始浏览器信息。

桌面按资料、安全、同步和会话四个区块排列；窄屏收为单列。视觉沿用既有浅色工作区、深色文字层级、绿色主操作和明确危险操作。页面应覆盖 loading、保存成功和接口不可用状态。
