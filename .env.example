# Copy this file to .env and fill in your values.
# .env is gitignored and never committed.

DIPOLE_AI_API_KEY=sk-your-api-key-here

# Required by docker-compose.microservices.yml. Replace before shared deployment.
DIPOLE_INTERNAL_RPC_SHARED_SECRET=replace-with-a-long-random-secret
