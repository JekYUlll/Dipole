services:
  agent:
    environment:
      # Compatibility overlay for existing deployment commands. The durable
      # read-active configuration is now owned by the base Compose file.
      DIPOLE_AGENT_RUNTIME_MODE: remote
