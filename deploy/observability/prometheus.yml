global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/kafka-alerts.yml

scrape_configs:
  - job_name: kafka
    static_configs:
      - targets:
          - kafka-exporter:9308
